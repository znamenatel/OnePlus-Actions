name: Build ReSukiSU-gt5pro (Clean)
on:
  workflow_dispatch:
    inputs:
      SUSFS:
        description: "是否启用SUSFS (Включить SUSFS?)"
        required: true
        type: choice
        options:
          - 'On'
          - 'Off'
        default: 'Off'

      lz4kd:
        description: "是否启用lz4kd (Включить lz4kd?)"
        required: true
        type: choice
        options:
          - 'On'
          - 'Off'
        default: 'Off'

      bbg:
        description: "是否启用BBG基带保护 (Включить защиту базовой полосы BBG?)"
        required: true
        type: choice
        options:
          - 'On'
          - 'Off'
        default: 'On'

      bbr:
        description: "是否启用BBR算法 (Включить алгоритм BBR?)"
        required: true
        type: choice
        options:
          - 'On'
          - 'Off'
        default: 'Off'

      proxy:
        description: "是否添加代理性能优化 (Включить оптимизацию прокси?)"
        required: true
        type: choice
        options:
          - 'On'
          - 'Off'
        default: 'On'

      UNICODE_BYPASS:
        description: "是否添加Unicode零宽绕过修复补丁 (Включить патч обхода Unicode?)"
        required: true
        type: choice
        options:
          - 'On'
          - 'Off'
        default: 'Off'

jobs:
  build:
    name: Build for gt5pro | SUSFS ${{ github.event.inputs.SUSFS }} | lz4kd ${{ github.event.inputs.lz4kd }}
    runs-on: ubuntu-latest
    env:
      CCACHE_COMPILERCHECK: "%compiler% -dumpmachine; %compiler% -dumpversion"
      CCACHE_NOHASHDIR: "true"
      CCACHE_HARDLINK: "true"
      CCACHE_MAXSIZE: 8G
      CCACHE_DIR: "/home/runner/.ccache_gt5pro_ReSukiSU"

    steps:
      - name: "⚙️ Setup Cache Env | Настройка окружения кэша"
        run: |
          echo "🔧 Настройка директории кэша для Realme GT5 Pro..."
          mkdir -p "$CCACHE_DIR"
          echo "✅ Директория кэша готова: $CCACHE_DIR"

      - name: "🔐 Configure Git | Настройка Git"
        run: |
          git config --global user.name "build"
          git config --global user.email "2210077278@qq.com"

      - name: "📦 Install Dependencies | Установка зависимостей"
        run: |
          sudo apt-get update
          sudo apt-get install -y python3 git curl ccache libelf-dev build-essential flex bison libssl-dev libncurses-dev liblz4-tool zlib1g-dev libxml2-utils rsync unzip gawk dos2unix clang lld device-tree-compiler

      - name: "💾 Restore ccache (With SUSFS) | Восстановление кэша (С SUSFS)"
        if: ${{ github.event.inputs.SUSFS == 'On' }}
        uses: actions/cache@v5
        with:
          path: ${{ env.CCACHE_DIR }}
          key: ccache-${{ runner.os }}-${{ github.ref }}-gt5pro-ReSukiSU-SUSFS
          restore-keys: |
            ccache-${{ runner.os }}-gt5pro-ReSukiSU-SUSFS
            ccache-${{ runner.os }}-ReSukiSU-SUSFS

      - name: "💾 Restore ccache (Without SUSFS) | Восстановление кэша (Без SUSFS)"
        if: ${{ github.event.inputs.SUSFS == 'Off' }}
        uses: actions/cache@v5
        with:
          path: ${{ env.CCACHE_DIR }}
          key: ccache-${{ runner.os }}-${{ github.ref }}-gt5pro-ReSukiSU-NoSUSFS
          restore-keys: |
            ccache-${{ runner.os }}-gt5pro-ReSukiSU-NoSUSFS
            ccache-${{ runner.os }}-ReSukiSU-NoSUSFS

      - name: "⚙️ Initialize ccache | Инициализация ccache"
        run: |
          INIT_FLAG="${{ env.CCACHE_DIR }}/.ccache_initialized"
          if command -v ccache >/dev/null 2>&1; then
            if [ ! -f "$INIT_FLAG" ]; then
              ccache -M ${{ env.CCACHE_MAXSIZE }}
              touch "$INIT_FLAG"
            fi
          fi
          ccache -s

      - name: "📥 Install Repo Tool | Установка Repo"
        run: |
          curl -fsSL https://storage.googleapis.com/git-repo-downloads/repo > ~/repo
          chmod a+x ~/repo
          sudo mv ~/repo /usr/local/bin/repo

      - name: "⬇️ Clone Kernel Source | Клонирование исходников ядра"
        run: |
          mkdir kernel_workspace && cd kernel_workspace
          echo "🌐 Инициализация репозитория для gt5pro (sm8650)..."
          repo init -u https://github.com/Xiaomichael/kernel_manifest.git -b refs/heads/oneplus/sm8650 -m oneplus_12_b.xml --depth=1
          
          echo "🔄 Синхронизация исходников..."
          for i in {1..5}; do
            repo sync -c -j2 --fail-fast --force-sync --no-clone-bundle --no-tags && break || {
              echo "⚠️ Ошибка сервера. Повтор через 30 секунд ($i из 5)..."
              sleep 30
            }
          done
          
          rm kernel_platform/common/android/abi_gki_protected_exports_* || echo "No protected exports!"
          rm kernel_platform/msm-kernel/android/abi_gki_protected_exports_* || echo "No protected exports!"
          
          echo "🔧 Настройка информации о версии ядра..."
          sed -i 's/ -dirty//g' kernel_platform/common/scripts/setlocalversion
          sed -i 's/ -dirty//g' kernel_platform/msm-kernel/scripts/setlocalversion
          sed -i 's/ -dirty//g' kernel_platform/external/dtc/scripts/setlocalversion
          sed -i '$i res=$(echo "$res" | sed '\''s/-dirty//g'\'')' kernel_platform/common/scripts/setlocalversion
          sed -i '$i res=$(echo "$res" | sed '\''s/-dirty//g'\'')' kernel_platform/msm-kernel/scripts/setlocalversion
          sed -i '$i res=$(echo "$res" | sed '\''s/-dirty//g'\'')' kernel_platform/external/dtc/scripts/setlocalversion
          
          sed -i '$s|echo "\$res"|echo "-android15-oki-xiaoxiaow"|' kernel_platform/common/scripts/setlocalversion
          sed -i '$s|echo "\$res"|echo "-android15-oki-xiaoxiaow"|' kernel_platform/msm-kernel/scripts/setlocalversion
          sed -i '$s|echo "\$res"|echo "-android15-oki-xiaoxiaow"|' kernel_platform/external/dtc/scripts/setlocalversion

      - name: "⚡ Setup Baseband-guard | Настройка Baseband-guard"
        if: ${{ github.event.inputs.bbg == 'On' }}
        run: |
          set -e
          cd kernel_workspace/kernel_platform/common
          curl -sSL https://github.com/vc-teahouse/Baseband-guard/raw/main/setup.sh -o setup.sh
          bash setup.sh

      - name: "⚡ Setup ReSukiSU | Настройка ReSukiSU"
        run: |
          cd kernel_workspace/kernel_platform
          curl -LSs "https://raw.githubusercontent.com/ReSukiSU/ReSukiSU/main/kernel/setup.sh" | bash -s main
          cd KernelSU
          KSU_VERSION=$(expr $(/usr/bin/git rev-list --count main) "+" 30700)
          echo "KSUVER=$KSU_VERSION" >> $GITHUB_ENV

      - name: "🔧 Setup Patches | Применение патчей"
        env:
          PYTHON_PATCH_SCRIPT: |
            import re
            import os
            import glob

            # Автоматически находим файл патча по маске
            patch_files = glob.glob("50_add_susfs_in_*.patch")
            patch_name = patch_files[0] if patch_files else "50_add_susfs_in_oki-android14-6.1.patch"

            if os.path.exists(patch_name):
                print(f"📦 Обработка файла патча: {patch_name}")
                with open(patch_name, "r") as f:
                    lines = f.readlines()
                new_lines = []
                skip = False
                for line in lines:
                    if (line.startswith("diff --git a/mm/memory.c") or line.startswith("diff --git a/common/mm/memory.c") or
                        line.startswith("diff --git a/fs/namespace.c") or line.startswith("diff --git a/common/fs/namespace.c") or
                        line.startswith("diff --git a/fs/stat.c") or line.startswith("diff --git a/common/fs/stat.c") or
                        line.startswith("diff --git a/fs/proc/task_mmu.c") or line.startswith("diff --git a/common/fs/proc/task_mmu.c")):
                        skip = True
                    elif line.startswith("diff --git "):
                        skip = False
                    if not skip:
                        new_lines.append(line)
                with open(patch_name, "w") as f:
                    f.writelines(new_lines)
                print("✅ Из файла патча успешно удалены конфликтные секции VFS и Procfs")

            if os.path.exists("mm/memory.c"):
                with open("mm/memory.c", "r") as f:
                    code = f.read()
                if "#include <linux/susfs.h>" not in code:
                    code = code.replace("#include <linux/memory.h>", "#include <linux/memory.h>\n#include <linux/susfs.h>")
                susfs_block = (
                    "#ifdef CONFIG_KSU_SUSFS_SUS_MAP\n"
                    "        struct page *page = NULL;\n\n"
                    "        if (vma && vma->vm_file && SUSFS_IS_INODE_SUS_MAP(file_inode(vma->vm_file)))\n"
                    "            break;\n\n"
                    "        page = get_user_page_vma_remote(mm, addr, gup_flags, &vma);\n"
                    "#else\n"
                    "        struct page *page = get_user_page_vma_remote(mm, addr, gup_flags, &vma);\n"
                    "#endif"
                )
                pattern = r"struct page\s*\*page\s*=\s*get_user_page_vma_remote\s*\(\s*mm\s*,\s*addr\s*,\s*\n?\s*gup_flags\s*,\s*&vma\s*\)\s*;"
                code, count = re.subn(pattern, susfs_block, code)
                if count == 0:
                    pattern_fallback = r"struct page\s*\*page\s*=\s*get_user_page_vma_remote\s*\([^;]+&vma\s*\)\s*;"
                    code, count = re.subn(pattern_fallback, susfs_block, code)
                with open("mm/memory.c", "w") as f:
                    f.write(code)
                print(f"✅ Файл mm/memory.c изменен напрямую. Замен: {count}")

            if os.path.exists("fs/namespace.c"):
                with open("fs/namespace.c", "r") as f:
                    code = f.read()
                if "CONFIG_KSU_SUSFS_SUS_MOUNT" not in code:
                    code = code.replace(
                        "#include <linux/mnt_idmapping.h>",
                        "#include <linux/mnt_idmapping.h>\n#ifdef CONFIG_KSU_SUSFS_SUS_MOUNT\n#include <linux/susfs_def.h>\n#endif"
                    )
                    susfs_ns_block = (
                        "#ifdef CONFIG_KSU_SUSFS_SUS_MOUNT\n"
                        "extern bool susfs_is_current_ksu_domain(void);\n"
                        "extern struct static_key_true susfs_is_sdcard_android_data_not_decrypted;\n\n"
                        "#define CL_COPY_MNT_NS_BIT(25) /* used by copy_mnt_ns() */\n\n"
                        "#endif\n\n"
                    )
                    code = code.replace("static unsigned int sysctl_mount_max", susfs_ns_block + "static unsigned int sysctl_mount_max")
                with open("fs/namespace.c", "w") as f:
                    f.write(code)
                print("✅ Файл fs/namespace.c успешно модифицирован напрямую")

            if os.path.exists("fs/stat.c"):
                with open("fs/stat.c", "r") as f:
                    code = f.read()
                if "CONFIG_KSU_SUSFS_SUS_KSTAT" not in code:
                    pattern_stat = r"if\s*\(\s*inode\s*->\s*i_op\s*->\s*getattr\s*\)\s*return\s+inode\s*->\s*i_op\s*->\s*getattr\s*\(\s*idmap\s*,\s*path\s*,\s*stat\s*,\s*request_mask\s*,\s*query_flags\s*\|\s*AT_GETATTR_NOSEC\s*\)\s*;"
                    susfs_stat_block = (
                        "if (inode->i_op->getattr)\n"
                        "#ifdef CONFIG_KSU_SUSFS_SUS_KSTAT\n"
                        "\t{\n"
                        "\t\tint err = inode->i_op->getattr(idmap, path, stat,\n"
                        "\t\t\t\t\t\t request_mask,\n"
                        "\t\t\t\t\t\t query_flags | AT_GETATTR_NOSEC);\n"
                        "\t\tif (!err)\n"
                        "\t\t\tsusfs_sys_kstat_spoof_generic_fillattr(inode, stat);\n"
                        "\t\treturn err;\n"
                        "\t}\n"
                        "#else\n"
                        "\t\treturn inode->i_op->getattr(idmap, path, stat,\n"
                        "\t\t\t\t\t\t     request_mask,\n"
                        "\t\t\t\t\t\t     query_flags | AT_GETATTR_NOSEC);\n"
                        "#endif"
                    )
                    code, count = re.subn(pattern_stat, susfs_stat_block, code)
                    print(f"✅ Файл fs/stat.c изменен напрямую. Замен: {count}")
                with open("fs/stat.c", "w") as f:
                    f.write(code)

            if os.path.exists("fs/proc/task_mmu.c"):
                with open("fs/proc/task_mmu.c", "r") as f:
                    code = f.read()
                if "CONFIG_KSU_SUSFS_SUS_MAP" not in code:
                    pattern_mmu = r"/\*\s*Case\s+4\s+above\s*\*/\s*if\s*\(\s*vma\s*->\s*vm_end\s*>\s*last_vma_end\s*\)\s*\{\s*smap_gather_stats\s*\(\s*vma\s*,\s*&mss\s*,\s*last_vma_end\s*\)\s*;\s*last_vma_end\s*=\s*vma\s*->\s*vm_end\s*;\s*\}"
                    susfs_mmu_block = (
                        "/* Case 4 above */\n"
                        "\t\tif (vma->vm_end > last_vma_end) {\n"
                        "#ifdef CONFIG_KSU_SUSFS_SUS_MAP\n"
                        "\t\t\tif (!vma->vm_file || !(SUSFS_IS_INODE_SUS_MAP(file_inode(vma->vm_file)))) {\n"
                        "\t\t\t\tsmap_gather_stats(vma, &mss, last_vma_end);\n"
                        "\t\t\t\tlast_vma_end = vma->vm_end;\n"
                        "\t\t\t}\n"
                        "#else\n"
                        "\t\t\tsmap_gather_stats(vma, &mss, last_vma_end);\n"
                        "\t\t\tlast_vma_end = vma->vm_end;\n"
                        "#endif\n"
                        "\t}"
                    )
                    code, count = re.subn(pattern_mmu, susfs_mmu_block, code)
                    print(f"✅ Файл fs/proc/task_mmu.c изменен напрямую. Замен: {count}")
                with open("fs/proc/task_mmu.c", "w") as f:
                    f.write(code)
        run: |
          cd kernel_workspace
          if [ "${{ github.event.inputs.SUSFS }}" == "On" ]; then
            git clone https://github.com/cctv18/susfs4oki.git -b oki-android14-6.1
          fi
          git clone https://github.com/Xiaomichael/kernel_patches.git
          git clone https://github.com/ShirkNeko/SukiSU_patch.git
          
          cd kernel_platform/common
          
          if [ "${{ github.event.inputs.SUSFS }}" == "On" ]; then
            cp ../../susfs4oki/kernel_patches/50_add_susfs_in_*.patch ./
            cp ../../susfs4oki/kernel_patches/fs/* ./fs/
            cp ../../susfs4oki/kernel_patches/include/linux/* ./include/linux/
          else
            cp ../../kernel_patches/sukisu/scope_min_manual_hooks_v1.9.patch ./
          fi
          cp ../../kernel_patches/zram_patches/001-lz4.patch ./
          cp ../../kernel_patches/zram_patches/lz4armv8.S ./lib/
          cp ../../kernel_patches/zram_patches/002-zstd.patch ./
          
          if [ "${{ github.event.inputs.UNICODE_BYPASS }}" = "On" ]; then
            cp ../../kernel_patches/common/unicode_bypass_fix_6.1+.patch ./unicode_bypass_fix.patch
          fi
          
          if [ "${{ github.event.inputs.lz4kd }}" = "Off" ]; then
            patch -p1 -F 3 < 001-lz4.patch || true
          fi
          
          if [ "${{ github.event.inputs.lz4kd }}" == "On" ]; then
            cp ../../SukiSU_patch/other/zram/zram_patch/6.1/lz4kd.patch ./
            patch -p1 -F 3 < lz4kd.patch || true
            cp ../../SukiSU_patch/other/zram/zram_patch/6.1/lz4k_oplus.patch ./
            patch -p1 -F 3 < lz4k_oplus.patch || true
          fi

          if [ "${{ github.event.inputs.SUSFS }}" == "On" ]; then
            printenv PYTHON_PATCH_SCRIPT > patch_all.py
            python3 patch_all.py
            rm patch_all.py
            
            REAL_PATCH=$(ls 50_add_susfs_in_*.patch | head -n 1)
            echo "🔥 Применяю патч: $REAL_PATCH"
            if ! patch -p1 -F 3 < "$REAL_PATCH"; then
              find . -name "*.rej" -exec echo "--- Файл: {} ---" \; -exec cat {} \;
              exit 1
            fi
          else
            patch -p1 -F 3 < scope_min_manual_hooks_v1.9.patch
          fi

          echo "🩹 Проверяю и чиню avc_has_perm в hooks.c..."
          HOOKS_FILE="security/selinux/hooks.c"
          if [ -f "$HOOKS_FILE" ]; then
            if grep -q "avc_has_perm(mysid, mysid, SECCLASS_PROCESS," "$HOOKS_FILE"; then
              sed -i 's/avc_has_perm(mysid, mysid, SECCLASS_PROCESS,/avc_has_perm(\&selinux_state, mysid, mysid, SECCLASS_PROCESS,/' "$HOOKS_FILE"
              echo "✅ hooks.c пофиксен (добавлен &selinux_state)"
            fi
          fi

          if [ "${{ github.event.inputs.SUSFS }}" == "On" ]; then
            FILES_TO_FIX=$(grep -rlE '\b(susfs_[a-zA-Z_]+|SUSFS_[A-Z_]+)\(' --include="*.c" fs/ security/ 2>/dev/null | xargs grep -L "linux/susfs.h" 2>/dev/null || true)
            if [ -n "$FILES_TO_FIX" ]; then
              for f in $FILES_TO_FIX; do
                LAST_INC=$(grep -n '^#include' "$f" | tail -1 | cut -d: -f1)
                if [ -n "$LAST_INC" ]; then
                  sed -i "${LAST_INC}a #include <linux/susfs.h>" "$f"
                fi
              done
            fi
          fi

          if [ -f "fs/stat.c" ] && ! grep -q "ksu_handle_stat" fs/stat.c; then
            find $GITHUB_WORKSPACE -path "*/KernelSU/kernel/tools/inline_hook_check.mk" -exec sed -i 's/\$(error/\$(warning/g' {} \;
          fi

          # 🔥 НОВОЕ: Автоматическая интеграция baseband_guard в конфигурацию безопасности LSM ядра
          echo "🛡️ Интегрирую Baseband-guard в список CONFIG_LSM..."
          find arch/arm64/configs/ -type f -exec sed -i 's/CONFIG_LSM="[^"]*/&,baseband_guard/' {} + 2>/dev/null || true
          find ../vendor/ -type f -name "*.config" -exec sed -i 's/CONFIG_LSM="[^"]*/&,baseband_guard/' {} + 2>/dev/null || true
      - name: "🔨 Build Kernel | Сборка ядра"
        run: |
          echo "🏗️ Начинаю сборку ядра..."
          export KBUILD_BUILD_USER="xiaoxiaow"
          export KBUILD_BUILD_HOST="xiaoxiaow_build"
          
          export PATH="$GITHUB_WORKSPACE/kernel_workspace/kernel_platform/prebuilts/clang/host/linux-x86/clang-r487747c/bin:$PATH"
          export PATH="/usr/lib/ccache:$PATH"
          
          sudo apt install -y libelf-dev
          cd $GITHUB_WORKSPACE/kernel_workspace/kernel_platform/common
          
          echo "📝 Шаг 1: Конфигурация ядра (Без параллельных потоков)..."
          make LLVM=1 ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- CC="ccache clang" O=out gki_defconfig
          
          echo "🚀 Шаг 2: Компиляция (Все доступные ядра)..."
          make -j$(nproc --all) LLVM=1 ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- CC="ccache clang" RUSTC=../../prebuilts/rust/linux-x86/1.73.0b/bin/rustc PAHOLE=../../prebuilts/kernel-build-tools/linux-x86/bin/pahole LD=ld.lld HOSTLD=ld.lld O=out KCFLAGS="-O2 -Wno-error" HOSTCFLAGS="-Wno-error" all

          ccache -s
          echo "✅ Процесс сборки завершен!"
